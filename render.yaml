services:
  - type: web
    name: muros-estates
    runtime: node
    plan: free
    buildCommand: npm install --include=dev && npx cross-env npx tsx script/build.ts
    startCommand: node dist/index.cjs
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        sync: false
